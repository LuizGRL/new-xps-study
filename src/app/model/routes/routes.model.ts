
import { RoutesLogin as LOGIN } from '@app-model/routes/routes-login.model';
import { RoutesCommon as COMMON } from '@app-model/routes/routes-common.models';

const routes = {
  LOGIN,
  COMMON
}

export function RoutesPath(){
  return routes;
}
